// Main entry point for root-level pages (depth: 1)
// Pages at this level: home/index.html, mission/index.html, ourPurpose/index.html

// Page depth: 1 (one level deep from root)
const PAGE_DEPTH = 1;

// Initialize all UI components
UIComponents.initializeAll(PAGE_DEPTH);