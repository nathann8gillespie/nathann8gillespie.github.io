// Main entry point for section pages (depth: 2)
// Pages at this level: sections/social-media/index.html, sections/medical/index.html, etc.

// Page depth: 2 (two levels deep from root)
const PAGE_DEPTH = 2;

// Initialize all UI components
UIComponents.initializeAll(PAGE_DEPTH);